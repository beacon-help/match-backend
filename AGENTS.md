# AGENTS

## Repository Interaction Rule

- Use `make` commands to interact with repository code whenever a corresponding target exists (for example: running tests, linting, formatting, and app tasks).
- Prefer `make <target>` over calling underlying tools directly.
